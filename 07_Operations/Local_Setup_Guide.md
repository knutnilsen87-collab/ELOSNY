# Local Setup Guide

## Prerequisites
- Node.js 24 or newer.
- npm 11 or newer.
- Git.

## Start steps
1. Open `10_Starter_Kit/repo`.
2. Run `npm install`.
3. Run `npm test`.
4. Run `npm start`.
5. Open `http://localhost:4173`.

## Verification commands

```powershell
npm test
npm run smoke
```
